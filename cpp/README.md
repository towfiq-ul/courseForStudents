# Beginner to Advance in Computer Programming in C++

## Class Topics:

### One and Two: 
	* structure of cpp program, code example ex1.cpp, header file, 
	* main(), return type / type of function, begginging of function, statements, function block, end of function, 
	* structure of simple cpp program ex2.cpp, OOP
	* fundamental types, 
		* boolean	: bool
		* characters	: char, wchar_t
		* integers	: short, int, long	
		* floating	: float, double, long double
	* signed modifiers, unsigned modifiers, operator, sizeof operator, 
	* constants : boolean, integral, floating point constants, character constants, string, 
	* escape sequences, variables, defination, initialization, const, volatile


### Three and Four:
	* Declaring Function: Function prototype, mathematical standard function, Declarations, Declaring Function
	* Function Calls, Type ```void``` for function, function without ```return``` 
	* Usage of ```srand()```, ```rand()```, 
	* Using header file, searching for header file, standard class definition, standard header files in cpp and c standard library
	* Using standard classes, creating objects, calling methods, classes and global functions, 
	* Streams: iostream, cin, cout, cerr, clog, i/o stream class, standard class
	* Formatting and manipulators, formatting, flags and manipulators, formatted output of integers, formatting options, numeric system, negative numbers,
	* Standard settings, formatting, 
	* Output characters, strings, and boolean values: outputting characters and character codes, Outputting strings, outputting bollean values
	* Formatted Input: input fields
	* Formatted Input of Numbers: inputting integers, inputting floating-point numbers, input error, 
	* Reading and Writing Characters, Reading a line, 


### Five and Six: 
	* Binary arithmetic operators: operators, operands, +, -, *, /, %, 
	* Binary arithmetic operators, expressions
	* unary arithmetic operators: +, -, ++, --
	* sign operator, increment / decrement operator, precedence, 
	* simple assignments: = 
	* compound assignments: +=, -=, *=, /= 
	* Relational operator: <, >, <=, >=, ==, !=
	* Result of comparison, precedence of relational operators, 
	* Logical Operators: &&, ||, &, |, !
	* Operands and order of evaluation, precedence of boolean operators,
	* Control Flow: loops, selection, jumps
	* loops: while, do-while, for
	* selection: if-else, switch, conditional operator ?:
	* jumps : goto, continue, break
	* Building blocks, nested if-else, loops, 


### Six and Seven:
